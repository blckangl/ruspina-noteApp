import { CheckedNoteDirective } from './checked-note.directive';

describe('CheckedNoteDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckedNoteDirective();
    expect(directive).toBeTruthy();
  });
});
